# se unicode-names

```
usage: unicode-names [-h] [STRING ...]

Display Unicode code points, descriptions, and links to more details for each
character in a string. Useful for differentiating between different flavors of
spaces, dashes, and invisible characters like word joiners.

positional arguments:
  STRING      a Unicode string

options:
  -h, --help  show this help message and exit
```
